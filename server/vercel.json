{
  "version": 2,
  "buildCommand": "npx prisma generate",
  "functions": {
    "api/index.ts": {
      "memory": 256,
      "maxDuration": 30
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "api/index.ts" }
  ]
}
